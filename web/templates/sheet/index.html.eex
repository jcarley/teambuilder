<h2>Listing sheets</h2>

<table class="table">
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Start date</th>
      <th>End date</th>

      <th></th>
    </tr>
  </thead>
  <tbody>
<%= for sheet <- @sheets do %>
    <tr>
      <td><%= sheet.name %></td>
      <td><%= sheet.description %></td>
      <td><%= sheet.start_date %></td>
      <td><%= sheet.end_date %></td>

      <td class="text-right">
        <%= link "Show", to: team_sheet_path(@conn, :show, @team, sheet), class: "btn btn-default btn-xs" %>
        <%= link "Edit", to: team_sheet_path(@conn, :edit, @team, sheet), class: "btn btn-default btn-xs" %>
        <%= link "Delete", to: team_sheet_path(@conn, :delete, @team, sheet), method: :delete, data: [confirm: "Are you sure?"], class: "btn btn-danger btn-xs" %>
      </td>
    </tr>
<% end %>
  </tbody>
</table>

<%= link "New sheet", to: team_sheet_path(@conn, :new, @team) %>
